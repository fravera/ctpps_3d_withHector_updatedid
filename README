To run the data unpacking (after modifying the input file name):
cmsRun ctppsPixelTest2.py (or ctppsPixelTest2_FF.py)

or

cmsRun ctppsPixelTest2_noDEBUG.py
(to have less messages).

To run the DQM (after modifying the input file name):
cmsRun pixel_dqm_test_cfg.py or pixel_dqm_test_FF_cfg.py


To run the digis directly from the RAW data:
cmsRun NewStreamIn2_FF_cfg.py

To run directly the DQM from the RAW data:
cmsRun pixel_dqm_test_FF_stream_cfg.py
